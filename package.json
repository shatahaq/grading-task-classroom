{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "echo \"No frontend build step is required. Native assets live in public/assets.\"",
        "build": "echo \"No frontend build step is required. Native assets live in public/assets.\""
    }
}
